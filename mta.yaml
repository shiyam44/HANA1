ID: GIT1
_schema-version: '2.1'
version: 0.0.1

modules:
 - name: GITDB
   type: hdb
   path: GITDB
   requires:
    - name: hdi_GITDB

resources:
 - name: hdi_GITDB
   properties:
      hdi-container-name: ${service-name}
   type: com.sap.xs.hdi-container
